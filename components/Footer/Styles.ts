import styled from 'styled-components';
import Image from 'next/image';

export const StyledFooterWrapper = styled.div`
  width: 100%;
  height: 100%;
  margin-top: 5rem;
`;

export const StyledFooterImager = styled(Image)`
  margin: -6rem 3rem 0 auto;
`;
